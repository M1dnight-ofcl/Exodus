print("client init")

-- ! some stuff idek (i dont need this)
-- ! local csg_evnt = require(game:GetService("StarterGui").ClassSelectorGui.Frame.events)
-- ! csg_evnt.closeMenu();
-- ! task.wait(2)
-- ! csg_evnt.openMenu();